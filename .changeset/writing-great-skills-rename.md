---
"mattpocock-skills": minor
---

**Breaking:** rename **`writing-for-agents`** back to **`writing-great-skills`** and restore the `GLOSSARY.md` split. The skill reverts to user-invoked (`disable-model-invocation: true`); the description becomes a human-facing summary, not a model trigger. The reference now covers the vocabulary and principles that make a skill predictable — invocation choice, information hierarchy, pruning, leading words, and failure modes — with full definitions disclosed to `GLOSSARY.md`. `SKILL-MECHANICS.md` is gone; that content is absorbed into the SKILL. `ask-matt`'s pointer updated. Reinstall under the new name; the old name is gone (no alias).
